# FastAPI + server
fastapi
uvicorn[standard]
pydantic
websockets
python-multipart
python-dotenv

# Pipeline + engine runtime deps (imported via sys.path, not installed as packages)
pandas>=2.0.0
pyarrow>=14.0.0
numpy>=1.26.0
yfinance>=0.2.28
